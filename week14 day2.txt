Encapsulation: Implementation is hidden from the client

3 pillars of OOP:
	encapsulation
	inheritance
	polymorphism

Ploymorphism
	method over loading
		multiple method of same name in same class
	
	method over riding
		overriding an inherited method
		@Override- use this		
			allow compiler to know what v r doing

child 1st calls constructor of parent class
	then it's itself		